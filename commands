# CONTRACT
docker run --name="hop_contract" -it -d "eu.gcr.io/hip-orbit-347017/hop_contract" python3 hop_contract.py "$(cat hop_contract.json)"

# To Copy .log file from container to host machine
# docker cp hop_contract:/hop_contract/logs/error.log .

# To run docker container checker
bash container_check
