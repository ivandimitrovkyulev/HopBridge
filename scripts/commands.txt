# For web screening
var="$(cat input.json)"
docker run --shm-size="2g" -it <image-id> python3 main.py "$var"

# For etherscan screening
var="$(cat contracts.json)"
docker run -it <image-id> python3 etherscan.py <mode> "$var"
